version: '3.9'

services:
  signaling:
    build:
      context: ./signaling-server
      dockerfile: Dockerfile
    image: oyot-backend:${TAG:-latest}
    ports:
      - "3001:3001"
    environment:
      PORT: "3001"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3001/health"]
      interval: 30s
      timeout: 10s
      retries: 3
