version: '3.9'

services:
  signaling:
    build:
      context: ./signaling-server
      dockerfile: Dockerfile
    image: oyot-backend:${TAG:-latest}
    ports:
      - "3001:3001"
    environment:
      PORT: "3001"
    restart: unless-stopped
