{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window (desktop only)",
  "platforms": ["macOS", "windows", "linux"],
  "windows": ["main"],
  "comment": [
    "No fs permissions: the webview never touches the filesystem. Picking an",
    "image yields a path from the dialog plugin, and the bytes are read by the",
    "import_image_from_path command in Rust, which is not subject to this ACL.",
    "Rendering an attachment goes through the asset protocol, scoped to the",
    "attachments directory in tauri.conf.json."
  ],
  "permissions": ["core:default", "opener:default", "dialog:default", "os:default"]
}
